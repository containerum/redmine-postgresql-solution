{
    "name": "{{.PG_SVC_NAME}}{{.HASH}}",
    "deploy": "{{.PG_DEPLOY_NAME}}{{.HASH}}",
    "ports": [{
        "name": "pg",
        "port": {{.PG_PORT}},
        "target_port": {{.PG_PORT}},
        "protocol": "TCP"
        }]
}