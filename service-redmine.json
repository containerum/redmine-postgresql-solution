{
    "kind": "Service",
    "metadata": {
        "labels": {
            "owner": "{{.OWNER}}"
        },
        "name": "{{.REDMINE_SVC_NAME}}-{{.REDMINE_SVC_HASH}}"
    },
    "spec": {
        "ports": [
            {
                "protocol": "TCP",
                "targetPort": {{.REDMINE_PORT}}
            }
        ]
    }
}
