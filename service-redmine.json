{
    "name": "{{.REDMINE_SVC_NAME}}{{.HASH}}",
    "deploy": "{{.REDMINE_DEPLOY_NAME}}{{.HASH}}",
    "ports": [{
        "name": "redmine",
        "target_port": {{.REDMINE_PORT}},
        "protocol": "TCP"
        }]
}
