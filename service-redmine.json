{
    "name": "{{.REDMINE_SVC_NAME}}-{{.REDMINE_SVC_HASH}}",
    "deploy": "{{.REDMINE_DEPLOY_NAME}}-{{.REDMINE_DEPLOY_HASH}}",
    "ports": [{
        "name": "redmine",
        "port": {{.REDMINE_PORT}},
        "target_port": {{.REDMINE_PORT}},
        "protocol": "TCP"
        }],
    "owner": "{{.OWNER}}"
}